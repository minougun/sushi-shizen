{
  "name": "sushi-zen-site",
  "private": true,
  "description": "Static site for Sushi Zen. Production: https://sushi-zen.net — `npm run check:i18n` then `npm run deploy:prod` (Vercel CLI).",
  "scripts": {
    "check:i18n": "node scripts/check-i18n-keys.js",
    "images:mobile": "node scripts/generate-mobile-images.cjs",
    "deploy:prod": "vercel deploy --prod"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
